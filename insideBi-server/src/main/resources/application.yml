server:
  port: ${PORT:8080}

spring:
  sql:
    init:
      mode: never

app:
  ai-backend-url: ${AI_BACKEND_URL:http://localhost:8000}
  admin-password: ${ADMIN_PASSWORD:1}
  cors-allowed-origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}
  rate-limit:
    max-requests: 10
    window-seconds: 60
